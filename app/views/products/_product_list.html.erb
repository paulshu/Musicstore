<ul class="productList clearfix">
  <% @products.each_with_index do |product, index| %>

    	<%= render :partial => "welcome/product_item", locals: { product: product, index: index } %>

  <% end %>
</ul>
